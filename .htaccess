RewriteEngine On

# Se o arquivo ou pasta existir, entrega direto
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Caso contrário, joga tudo para o index.php
RewriteRule ^ index.php [QSA,L]
